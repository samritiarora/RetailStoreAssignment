package enterprise.org.assignment.commons;

import java.math.BigDecimal;

public final class Constants {

    public static final BigDecimal PERCENT = BigDecimal.valueOf(100);

    private Constants() {
        // Shouldn't be any need to instantiate.
    }
}
